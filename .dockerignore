# Runtime-only image: the build context needs nothing but the goreleaser-built
# binary (see Dockerfile, which only COPYs `triagearr`). Allowlist model —
# exclude everything, then let the binary back in.
*
!triagearr
